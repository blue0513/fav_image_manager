<!DOCTYPE html>
<html>
  <head>
    <title>FavImageManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <header>
      <div class="navbar navbar-dark bg-dark box-shadow">
        <div class="container d-flex justify-content-between">
          <%= link_to "FavImageManager", root_path, {class: "navbar-brand d-flex align-items-center"} %>
        </div>
      </div>
    </header>

    <main role="main">

      <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Fav Images</h1>
          <p class="lead text-muted">Your favorite images are here!</p>
          <p>
              <%= link_to("Upload FavImage", fav_images_new_path, {class: "btn btn-primary my-2"}) %>
              <%= link_to("Bulk Upload FavImages", fav_images_bulk_new_path, {class: "btn btn-primary my-2"}) %>
          </p>
        </div>
      </section>

      <%= yield %>

    </main>
  </body>
</html>
